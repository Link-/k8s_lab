pipeline {
  agent any

  environment {
    SERVICE_PORT        = '3006'
    SERVICE_HEALTH_PORT = '9006'
  }

  stages {
    stage('Build') {
      steps {
        sh 'echo STARTING BUILD ...'
      }
    }
  }
}